// 统一中转导出
// 使用方法：
// import {request} from '@/utils'
import { request } from "./reuqest";
import { getToken, setToken, removeToken } from './token'

export {
    request,
    getToken, 
    setToken, 
    removeToken
}